#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
   new matrix = []
   for row in matrix:
    new_row = []
       for x in row:
            new row.append(x**2)
        new_matrix.append(new_row)
    return new_matrix
